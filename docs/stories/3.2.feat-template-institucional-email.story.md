---
epic_num: 3
story_num: 2
story_title_short: "feat-template-institucional-email"
status: "Ready for Review"
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["cd backend && npm run lint", "cd backend && npm run build", "cd backend && npm test"]
---

# Story 3.2: Template institucional de header e rodape para e-mails

**Epic:** BulkMail Pro - System Management
**Story ID:** 3.2
**Sprint:** 3
**Priority:** 🔴 High
**Points:** 5
**Effort:** 4-6 hours
**Status:** Ready for Review
**Type:** 💻 Feature

---

## 🏗️ Executor Assignment (Projeto Bob)

**Work Type:** Code/Features/Logic
**Executor:** @dev
**Quality Gate:** @architect
**Quality Gate Tools:** ["cd backend && npm run lint", "cd backend && npm run build", "cd backend && npm test"]

---

## 🔀 Cross-Story Decisions

| Decision | Source | Impact on This Story |
|----------|--------|----------------------|
| Quill stores email body as HTML | Story 1.5 | Template must wrap existing HTML without breaking editor output |
| SMTP config can come from database | Story 3.1 | Sender name/email should continue using dynamic SMTP configuration |
| Queue sends per-recipient jobs | `backend/src/queue/processors.ts` | Template rendering must happen before `transporter.sendMail` for each recipient |
| Idempotency uses campaign, recipient, subject and sender | `backend/src/controllers/sendController.ts` | Template decoration must not change dedupe semantics |

---

## 📋 User Story

**Como** administrador do CREFITO11,
**Quero** que os e-mails enviados pelo sistema tenham header e rodape institucionais com a marca do CREFITO11,
**Para** que as mensagens deixem de parecer texto simples e passem credibilidade visual alinhada ao manual da marca.

---

## 🎯 Objective

Criar um template HTML institucional reutilizavel para todos os e-mails enviados pelo BulkMail Pro, envolvendo o conteudo criado no Quill com header, logo, area de corpo e rodape alinhados ao manual da marca em `assets/CREFITO 11 - Manual da Marca.pdf` e usando arquivos de logo de `assets/Logos crefito11 png`.

O template deve preservar o conteudo HTML atual do usuario, manter fallback em texto puro quando possivel e funcionar em clientes de e-mail comuns com CSS inline/table layout.

---

## ✅ Tasks

### Phase 1: Analise de marca e ativos (1 hour)

- [x] **1.1** Revisar `assets/CREFITO 11 - Manual da Marca.pdf` e registrar no codigo ou teste os tokens visuais usados no e-mail (cores, fonte segura equivalente, espacamento e regra de area de protecao)
- [x] **1.2** Selecionar logo principal a partir de `assets/Logos crefito11 png`, preferindo `CREFITO 11 - Marca - Pos Completa.png` para fundo claro
- [x] **1.3** Definir estrategia de logo para e-mail: anexo inline via CID no Nodemailer ou URL absoluta publica, evitando caminho local que nao renderiza no destinatario

### Phase 2: Backend template renderer (2 hours)

- [x] **2.1** Criar um servico de template de e-mail no backend para envolver `html` com header, corpo e rodape institucional
- [x] **2.2** Garantir que o renderer aceite o HTML gerado pelo Quill e mantenha variaveis `{{campo}}` funcionando antes/depois do wrap conforme fluxo existente
- [x] **2.3** Implementar rodape com identificacao institucional do CREFITO11 e texto discreto de mensagem automatica
- [x] **2.4** Gerar fallback `text` quando o payload nao trouxer texto puro, removendo tags HTML de forma segura
- [x] **2.5** Integrar o renderer em `backend/src/queue/processors.ts` antes de `transporter.sendMail`

### Phase 3: Integracao de envio e testes (2 hours)

- [x] **3.1** Ajustar tipos de `EmailJobData` somente se necessario para suportar assets inline/metadata do template
- [x] **3.2** Adicionar testes unitarios para validar que o HTML final contem header, logo, corpo original e rodape
- [x] **3.3** Adicionar teste para garantir que variaveis `{{campo}}` continuam sendo substituidas no HTML final
- [x] **3.4** Adicionar teste para garantir que o payload do Nodemailer usa `attachments` com `cid` quando a estrategia inline for adotada
- [ ] **3.5** Validar manualmente um envio de teste via configuracao SMTP e verificar renderizacao do logo no cliente de e-mail

---

## 🎯 Acceptance Criteria

```gherkin
GIVEN um usuario compoe um e-mail com conteudo HTML no editor Quill
WHEN o sistema processa o envio
THEN o HTML enviado pelo Nodemailer deve conter um header institucional do CREFITO11
AND deve preservar integralmente o conteudo criado pelo usuario no corpo da mensagem
AND deve conter um rodape institucional

GIVEN o template usa a logo do CREFITO11
WHEN o destinatario abre o e-mail
THEN a logo deve renderizar por CID inline ou URL absoluta publica
AND o HTML nao deve referenciar caminhos locais como "assets/..."

GIVEN o corpo do e-mail possui variaveis como "{{nome}}"
WHEN o job e processado com variables.nome preenchido
THEN a mensagem final deve substituir a variavel no conteudo enviado
AND nao deve quebrar o header ou o rodape do template

GIVEN o payload de envio nao traz campo text
WHEN o e-mail e enviado
THEN o sistema deve gerar fallback em texto puro a partir do conteudo final
AND o envio deve continuar aceito por clientes que bloqueiam HTML
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml.

### Story Type Analysis

| Attribute | Value | Rationale |
|-----------|-------|-----------|
| Type | Backend/Email Feature | Altera o HTML final enviado pelo worker |
| Complexity | Medium | Exige compatibilidade com clientes de e-mail e Nodemailer attachments |
| Test Requirements | Unit, Integration/Manual | Template HTML e payload de envio precisam de validacao |
| Review Focus | Deliverability & Rendering | Evitar imagens quebradas, CSS externo e HTML incompatível |

### Agent Assignment

| Role | Agent | Responsibility |
|------|-------|----------------|
| Primary | @dev | Implementar renderer, integracao com worker e testes |
| Review | @qa | Validar criterios de aceite e renderizacao do e-mail |

---

## 🔗 Dependencies

**Blocked by:**
- Assets de marca disponiveis em `assets/CREFITO 11 - Manual da Marca.pdf` e `assets/Logos crefito11 png`

**Blocks:**
- Futuras melhorias de templates editaveis/versionados com identidade visual padrao

---

## ⚠️ Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| Logo nao renderiza no cliente de e-mail | High | Usar CID inline ou URL publica absoluta; cobrir em teste de payload |
| CSS moderno ser removido por clientes de e-mail | Medium | Usar HTML table-based e CSS inline |
| Manual da marca nao ser seguido com precisao | Medium | Extrair tokens visuais diretamente do PDF e documentar no renderer/testes |
| Template quebrar variaveis existentes | High | Manter substituicao de `{{campo}}` coberta por teste |
| Aumentar chance de spam por HTML malformado | Medium | Gerar HTML simples, sem scripts, fontes externas ou imagens remotas desnecessarias |

---

## 📋 Definition of Done

- [x] Template institucional aplicado automaticamente aos envios HTML
- [x] Header e rodape usam logo/estilo derivados dos assets oficiais do CREFITO11
- [x] Logo renderiza sem depender de caminho local
- [x] Conteudo HTML do usuario continua preservado
- [x] Fallback `text` gerado quando ausente
- [x] Testes unitarios cobrindo renderer, variaveis e payload de envio
- [x] `cd backend && npm run lint`, `cd backend && npm run build` e `cd backend && npm test` executados
- [x] File List atualizada pelo @dev ao implementar

---

## 📝 Dev Notes

### Key Files

- `backend/src/queue/processors.ts`: ponto atual que chama `transporter.sendMail`
- `backend/src/queue/emailQueue.ts`: tipos de job de e-mail
- `backend/src/controllers/sendController.ts`: recebe `html`, `text`, `templateId` e `variables`
- `backend/src/services/idempotencyService.ts`: ja contem helpers de variaveis de template e sanitizacao basica de HTML
- `assets/CREFITO 11 - Manual da Marca.pdf`: fonte para tokens visuais oficiais
- `assets/Logos crefito11 png/CREFITO 11 - Marca - Pos Completa.png`: candidato principal para header em fundo claro

### Technical Notes

- O renderer deve ser centralizado no backend para garantir que todos os envios, incluindo campanhas em fila, recebam o mesmo template.
- Para compatibilidade com e-mail, preferir HTML simples com tabelas, largura maxima em torno de 600px e CSS inline.
- Se usar CID inline, o `sendMail` deve receber `attachments: [{ filename, path, cid }]` e o HTML deve referenciar `src="cid:<cid>"`.
- Nao inserir scripts, CSS externo ou dependencia de fontes remotas no e-mail.
- O template deve continuar permitindo conteudo vindo do Quill, mas qualquer sanitizacao deve respeitar a story 1.5 e nao remover formatacao basica esperada.

### Testing

- Unit: renderer retorna HTML com header, body e footer.
- Unit: renderer nao referencia `assets/...` diretamente no atributo `src`.
- Unit: substituicao de variaveis continua funcionando.
- Unit/integration: `sendEmail` envia payload para Nodemailer com `html`, `text` e `attachments` quando aplicavel.
- Manual: envio de teste para conta real validando logo, layout e fallback.

### File List

> Atualizar durante a implementacao.

**Created:**
- `backend/src/services/emailTemplateService.ts`
- `backend/src/services/emailTemplateService.test.ts`
- `backend/src/queue/processors.send.test.ts`

**Modified:**
- `.env.example`
- `README.md`
- `backend/src/app.ts`
- `backend/src/queue/processors.ts`
- `backend/src/queue/processors.send.test.ts`
- `backend/src/services/emailTemplateService.ts`
- `backend/src/services/emailTemplateService.test.ts`
- `docker-compose.prod.yml`
- `docker-compose.yml`
- `frontend/nginx.conf`
- `docs/stories/3.2.feat-template-institucional-email.story.md`

---

## 🧑‍💻 Dev Agent Record

> This section is populated when @dev executes the story.

### Agent Model Used
GPT-5 Codex

### Execution Log
| Timestamp | Phase | Action | Result |
|-----------|-------|--------|--------|
| 2026-05-25 | Phase 1 | Reviewed story assets and backend email flow | Identified `processors.ts` as the SMTP integration point and selected positive complete CREFITO11 logo for CID inline use |
| 2026-05-25 | Phase 2 | Implemented institutional email renderer | Added branded table-based HTML, sanitized Quill body, CID logo attachment and text fallback |
| 2026-05-25 | Phase 2 | Integrated renderer into `sendEmail` | Nodemailer now receives templated `html`, fallback `text` and `attachments` |
| 2026-05-25 | Phase 3 | Added tests | Covered renderer, variable substitution path, CID logo and no local asset URL in HTML |
| 2026-05-25 | Validation | Ran backend quality gates | `npm run lint` passed with existing warnings, `npm run build` passed, `npm test` passed with 97 tests |
| 2026-08-06 | QA Fix | Restored the institutional image using the supplied asset | Renderer now emits a configurable absolute public URL and rejects unsafe URL protocols |
| 2026-08-06 | Runtime | Added the branded asset endpoint and proxy routing | `/api/assets/crefito11-email-logo.png` serves the exact 709x255 PNG through backend and frontend |
| 2026-08-06 | Validation | Re-ran backend and Compose quality gates | Lint/build passed, 17 suites and 118 tests passed, and both Compose files validated |
| 2026-08-06 | Visual | Recreated running services and rendered the email in Chromium | Container endpoint returned the exact source SHA-1; logo, body and footer rendered correctly |

### Completion Notes
- Institutional template is applied centrally in the backend worker path before `transporter.sendMail`.
- Logo uses the exact supplied `assets/logos/CREFITO 11 - Marca - Neg 2 Completa.png` through a public absolute URL, avoiding CID and local paths in the delivered HTML.
- `CREFITO11_LOGO_PATH` configures the backend source asset and `CREFITO11_LOGO_URL` configures the externally reachable URL used by the worker.
- The frontend proxy uses a priority `/api` location so image extensions under `/api/assets` are forwarded to the backend instead of being treated as SPA files.
- The running backend, worker and frontend containers were recreated and the current frontend build/nginx configuration was loaded for visual validation.
- A durable Docker image rebuild was attempted, but Docker Hub timed out while fetching the uncached `node:18-alpine` metadata; the active containers are updated, while rebuilding immutable images remains dependent on registry availability.
- `npm ci` required network approval after the existing empty `backend/node_modules` directory was owned by `nobody`; it was temporarily renamed so a new dependency install could be created.
- Real SMTP inbox validation was not executed because no usable mailbox validation flow is configured in this environment; Chromium visual validation passed, and a real recipient remains required for task 3.5.

### File List
- `.env.example`
- `README.md`
- `backend/src/app.ts`
- `backend/src/services/emailTemplateService.ts`
- `backend/src/services/emailTemplateService.test.ts`
- `backend/src/queue/processors.ts`
- `backend/src/queue/processors.send.test.ts`
- `docker-compose.prod.yml`
- `docker-compose.yml`
- `frontend/nginx.conf`
- `docs/stories/3.2.feat-template-institucional-email.story.md`

### Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-25 | 1.0 | Implemented institutional template using CID inline logo | Dex |
| 2026-08-06 | 1.1 | Restored the official supplied logo through a configurable public URL and added container visual validation | Dex |

---

## 🧪 QA Results

### Review Date: 2026-08-06

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

O renderer institucional está centralizado, sanitiza o HTML, preserva variáveis e gera fallback textual. Lint, build e 117 testes do backend passaram. Entretanto, a implementação atual foi alterada pela integração Mailgrid e substituiu a imagem institucional por texto `CREFITO11`.

### Compliance Check

- Coding Standards: ✓ lint e build passaram.
- Project Structure: ✓ renderer e testes permanecem no backend.
- Testing Strategy: ✗ a validação manual em cliente real continua pendente.
- All ACs Met: ✗ o critério de logo por CID ou URL absoluta não é atendido.

### Blocking Findings

- **HIGH**: `emailTemplateService.ts` gera somente um `<strong>CREFITO11</strong>` e não inclui imagem institucional.
- **HIGH**: os testes atuais exigem `not.toContain("cid:")`, confirmando comportamento oposto ao Acceptance Criterion da logo.
- **MEDIUM**: a tarefa 3.5 de envio manual por provedor configurado não foi executada.

### Security Review

A sanitização de HTML permanece coberta e não foram identificados novos riscos críticos.

### Performance Considerations

O template é construído em memória por destinatário; não há regressão relevante identificada na suíte atual.

---

### Re-review Date: 2026-08-06

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Os dois achados HIGH da revisão anterior foram corrigidos. O header voltou a usar uma imagem institucional real, agora por URL absoluta pública configurável, com fallback seguro para a URL de produção. O arquivo fornecido pelo usuário é servido por endpoint próprio e o proxy do frontend preserva corretamente a rota `/api/assets/...`.

### Requirements Traceability

- AC 1: coberto pelos testes do renderer e do processor, que verificam header, corpo original e rodapé.
- AC 2: coberto por testes da URL absoluta, ausência de CID/caminho local e fallback para configuração insegura; o PNG servido pelo container possui o mesmo SHA-1 do asset original.
- AC 3: coberto pelo teste de processamento com substituição de variáveis.
- AC 4: coberto pelos testes de geração do fallback textual.

### Validation Evidence

- `backend`: lint e build aprovados; 17 suítes e 118 testes aprovados.
- `docker-compose.yml` e `docker-compose.prod.yml`: configuração validada.
- Container atualizado e saudável; logo acessível direta e indiretamente pelo frontend.
- Asset retornado: PNG RGBA 709x255, idêntico ao arquivo fornecido (`SHA-1 2a426d615d8c4273a6ca08f1473422bb02d458c7`).
- Template completo renderizado e inspecionado em Chromium, com logo, corpo e rodapé visíveis.

### Remaining Concern

- **MEDIUM**: a tarefa 3.5 ainda requer um envio para caixa postal real para validar o comportamento final de um cliente de e-mail e o carregamento remoto de imagens. A evidência em Chromium reduz o risco, mas não substitui Gmail, Outlook ou equivalente.

### Gate Status

**CONCERNS** — critérios funcionais e automatizados atendidos; recomenda-se concluir a validação manual em cliente de e-mail antes de mover a story para Done.

### Gate Status

Gate: FAIL → `docs/qa/gates/3.2-feat-template-institucional-email.yml`

### Recommended Status

✗ Changes Required — definir uma estratégia de imagem suportada pelo Mailgrid e validar uma mensagem real antes de marcar Done.

---

## 📜 Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-25 | 1.0.0 | Initial draft for institutional CREFITO11 email template story | Orion |
| 2026-05-25 | 1.1.0 | Validated GO (8/10) — Status: Draft → Ready | @po |
| 2026-05-25 | 1.2.0 | Implemented institutional email template and marked Ready for Review | @dev |

---

**Criado por:** Orion (AIOX Master)  
**Data:** 2026-05-25
